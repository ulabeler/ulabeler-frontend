import {css} from '@emotion/react';

export default {
  header: css({
    width: '100%',
    height: '150px',
    alignItems: 'flex-start',
    justifyContent: 'flex-end',
  }),
  logo: css({
    width: '20%',
    height: '20%',
    textAlign: 'left',
    marginRight: 'auto',
    // imgTag
    img: css({
      width: '100%',
      height: '100%',
      textAlign: 'left',
      marginRight: 'auto',
    }),
  }),
};
